{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "build": {
    "env": {
      "NPM_FLAGS": "--force"
    }  
  },
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" }
  ]
}
